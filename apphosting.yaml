runConfig:
  cpu: 1
  memoryMiB: 512
  maxInstances: 2

scripts:
  buildCommand: pip install -r requirements.txt
  runCommand: gunicorn --bind :$PORT app:app

env:
  - variable: OPENAI_API_KEY
    secret: OPENAI_API_KEY
